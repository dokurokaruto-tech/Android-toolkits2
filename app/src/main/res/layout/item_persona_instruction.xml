<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:padding="4dp"
    android:layout_marginBottom="8dp"
    android:background="@drawable/bg_persona_instruction_md3">

    <!-- 有効 / 無効切り替えチェックボックス -->
    <com.google.android.material.checkbox.MaterialCheckBox
        android:id="@+id/cb_enabled"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@string/persona_enable_instruction"
        app:useMaterialThemeColors="true"
        android:layout_marginEnd="0dp" />

    <!-- 指示テキスト表示枠 -->
    <TextView
        android:id="@+id/tv_instruction_content"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:background="?attr/selectableItemBackground"
        android:hint="@string/persona_instruction_placeholder"
        android:textColor="?attr/colorOnSurface"
        android:textColorHint="?attr/colorOnSurfaceVariant"
        android:textSize="14sp"
        android:minHeight="48dp"
        android:gravity="center_vertical|start"
        android:maxLines="3"
        android:ellipsize="end"
        android:padding="8dp"
        android:clickable="true"
        android:focusable="true" />

    <!-- 削除ボタン -->
    <ImageButton
        android:id="@+id/btn_delete_instruction"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:src="@drawable/ic_md3_delete"
        app:tint="?attr/colorOnSurfaceVariant"
        android:contentDescription="@string/persona_delete_instruction" />

    <!-- 右端のドラッグハンドル -->
    <ImageButton
        android:id="@+id/tv_drag_handle"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:src="@drawable/ic_md3_drag_handle"
        android:contentDescription="@string/persona_reorder_instruction"
        app:tint="?attr/colorOnSurfaceVariant" />

</LinearLayout>
