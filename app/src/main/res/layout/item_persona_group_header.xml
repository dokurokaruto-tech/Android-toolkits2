<?xml version="1.0" encoding="utf-8"?>
<!-- カテゴリー見出し行。右の + で、この枠にプールから1本登録する。 -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingTop="10dp"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingStart="12dp"
    android:paddingEnd="4dp">

    <View
        android:layout_width="3dp"
        android:layout_height="22dp"
        android:layout_marginEnd="10dp"
        android:background="?attr/colorPrimary" />

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/tv_persona_group_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:ellipsize="end"
        android:maxLines="1"
        android:textAppearance="?attr/textAppearanceTitleSmall"
        android:textColor="?attr/colorOnSurface"
        tools:text="社会的な立場" />

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/tv_persona_group_summary"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="4dp"
        android:textAppearance="?attr/textAppearanceLabelSmall"
        android:textColor="?attr/colorOnSurfaceVariant"
        tools:text="2件中 1件が有効" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_persona_group_add"
        style="@style/Widget.Material3.Button.IconButton"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:contentDescription="@string/persona_group_add"
        app:icon="@drawable/ic_md3_add"
        app:iconSize="20dp"
        app:iconTint="?attr/colorPrimary" />
</LinearLayout>
