<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#1C1B1F">

    <!-- 既存の日付フォルダ選択UIをそのまま利用する。 -->
    <include layout="@layout/dialog_tag_picker" />

    <TextView
        android:id="@+id/tv_generated_folders_empty"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:padding="24dp"
        android:text="まだ画像が生成されていません。\nPC生成エージェントの接続を確認してください。"
        android:textColor="#CAC4D0"
        android:textSize="15sp"
        android:visibility="gone" />

    <FrameLayout
        android:id="@+id/generated_folders_loading_overlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#991C1B1F"
        android:clickable="true"
        android:focusable="true">

        <ProgressBar
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_gravity="center"
            android:indeterminateTint="#D0BCFF" />
    </FrameLayout>

</FrameLayout>
